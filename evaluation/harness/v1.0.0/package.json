{
  "name": "@ushso/o8-evaluation-harness",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic offline evaluation harness for question-to-data discovery outputs.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node tools/validate-package.mjs --read-only"
  }
}
