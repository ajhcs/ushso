{
  "name": "@ushso/retrieval-evaluator-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Four-lane, pre-tuning corpus and runtime attribution bridge for evaluator v2.",
  "scripts": {
    "build": "node tools/run-bridge.mjs --write",
    "test": "node --test tests/*.test.mjs",
    "validate": "node tools/validate-bridge.mjs"
  }
}
