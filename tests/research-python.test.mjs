import test from 'node:test';import assert from 'node:assert/strict';import {execFileSync} from 'node:child_process';import {fileURLToPath} from 'node:url';import {requireResearchPython} from '../scripts/research/research-python.mjs';
test('explicit absolute interpreter required without fallback',()=>{for(const env of [{},{USHSO_RESEARCH_PYTHON:'python3'},{USHSO_RESEARCH_PYTHON:''},{USHSO_RESEARCH_PYTHON:'/bad\0path'}])assert.throws(()=>requireResearchPython(env),/RESEARCH_PYTHON_ABSOLUTE_PATH_REQUIRED/);assert.equal(requireResearchPython({USHSO_RESEARCH_PYTHON:'/qualified/bin/python'}),'/qualified/bin/python');});
test('every PDF entry rejects missing dependency without fallback',()=>{for(const file of ['pdf-text.py','pdf-layout.py','cms-grid-extract.py']){try{execFileSync('/usr/bin/python3',['-I','-S',fileURLToPath(new URL('../scripts/research/'+file,import.meta.url)),'missing.pdf'],{encoding:'utf8'});assert.fail('unqualified parser accepted');}catch(error){assert.equal(error.status,1);assert.match(error.stdout,/ModuleNotFoundError/);}}});
