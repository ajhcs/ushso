// Reuse the actual runtime-envelope tests so this package independently verifies
// all nine response schemas, version separation and safety fallback semantics.
import '../../../../tests/quota-successor.test.mjs';
