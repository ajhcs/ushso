{
  "name": "@hackyard/observatory-core-contract",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Offline canonical metadata truth contract for Observatory objects, claims, access, evidence, and relationships.",
  "dependencies": {"ajv": "8.17.1"},
  "scripts": {
    "build:fixture": "node tools/build-fixtures.mjs --fixture",
    "manifest": "node tools/build-manifest.mjs",
    "validate": "node tools/validate-package.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
