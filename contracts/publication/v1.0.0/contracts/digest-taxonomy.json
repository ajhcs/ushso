{
  "taxonomy_version": "publication-digest-taxonomy.v1",
  "canonicalization": {
    "name": "ushso-canonical-json-v1",
    "encoding": "utf-8",
    "object_key_order": "unicode-code-point-ascending",
    "array_order": "preserved",
    "whitespace": "none",
    "number_and_scalar_encoding": "ecmascript-json-stringify",
    "domain_separation": "utf8(domain_prefix + newline + canonical_json)"
  },
  "algorithm": "sha256",
  "domains": [
    {"name": "canonical_revision_membership", "prefix": "ushso:publication:canonical_revision_membership:v1", "canonical_value": "selection model, canonical as-of, declared ordering, and exact ordered members", "excluded_paths": ["/manifest_id", "/sealed_at", "/revision_count", "/projection_obligation_count", "/membership_digest", "/immutable"]},
    {"name": "projection_document", "prefix": "ushso:publication:projection_document:v1", "canonical_value": "semantic projection envelope and content", "excluded_paths": ["/generation_id", "/projected_at", "/document_checksum", "/immutable"]},
    {"name": "projection_set", "prefix": "ushso:publication:projection_set:v1", "canonical_value": "document IDs and checksums ordered by document ID", "excluded_paths": []},
    {"name": "component_generation", "prefix": "ushso:publication:component_generation:v1", "canonical_value": "component kind, W1, projector, schema, documents, and normalized acknowledgements", "excluded_paths": ["/generation_id", "/sealed_at", "/retention", "/component_checksum", "/immutable"]},
    {"name": "full_snapshot_build", "prefix": "ushso:publication:full_snapshot_build:v1", "canonical_value": "W1, all ordered component checksums, and reconciled counts", "excluded_paths": ["/receipt_id", "/previous_publication_ref", "/repeatability", "/started_at", "/sealed_at", "/deterministic_build_checksum", "/immutable"]},
    {"name": "publication_manifest", "prefix": "ushso:publication:publication_manifest:v1", "canonical_value": "entire sealed manifest other than its digest and immutable marker", "excluded_paths": ["/publication_digest", "/immutable"]},
    {"name": "legacy_static_compatibility", "prefix": "ushso:publication:legacy_static_compatibility:v1", "canonical_value": "entire legacy compatibility manifest other than its digest and immutable marker", "excluded_paths": ["/manifest_digest", "/immutable"]},
    {"name": "package_manifest", "prefix": "ushso:publication:package_manifest:v1", "canonical_value": "ordered exact-byte file entries and package metadata", "excluded_paths": []}
  ],
  "immutable": true
}
