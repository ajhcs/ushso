# PR-NEW — Concrete source or implementation deficit

Parent requirement(s), finding(s), phase/sub-phase:

Exact failing source/fixture/record/context and evidence hash:

Why the existing PR cannot close this deficit within its bounded scope:

Dependencies and downstream gate that must wait:

Owned files and expected resulting behavior:

1. Atomic commit: narrow schema/fixture/data-contract change with its check.
2. Atomic commit: specific parser/adapter/behavior change with its regression evidence.
3. Atomic commit: integration/publication/guide change with an actual consumer check.

Acceptance: state a result that could fail and the evidence required to decide it.

Handoff, GitHub submission and independent-review requirements follow EXECUTION.md. Add the stable ID and edges to plan-source.py, update the validator's declared initial sequence when intentionally expanding the program, regenerate and validate. Do not waive the original target by omitting this case from the denominator.
