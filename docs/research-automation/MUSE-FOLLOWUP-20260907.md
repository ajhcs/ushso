# Muse follow-up — 2026-09-07

Historical follow-up packet. See [current implementation-plan review](IMPLEMENTATION-PLAN-20260907.md) for current dictionary counts, actual Astra/browser checks, minimum-Node qualification, the newest latency sample and the distinct new Muse runtime failure. Original evidence below remains attributed to its original run.

Engineering and verification remain incomplete. Scientific approval and deployment remain owner decisions; no canonical catalog changes, approved receipts or deployment were issued.

Current evidence is retained under `/mnt/d/tmp/plumbob/ushso-research-expansion-20260907`:

- `review/post-muse/completeness-report.md`: corrected full-corpus counts, before/after catalog index and unresolved-record inventory, generated after the current sweep completes.
- `review/FOLLOWUP-20260907.md`: exact current verification status and outstanding work.
- `muse-review`, `muse-api-review`, `muse-followup-review`: three actual DSH/OpenRouter prompt-only reviews of hash-inventoried allowlisted code and public publisher metadata. Credentials, private correspondence, patient data and restricted payloads were excluded. Model findings retain evidence, severity and proposed fixes; they are not automatically established defects.
- `post-muse-qa`: separate post-change test, browser, WebMCP and Astra evidence. Preserve earlier attempts, including failures.

Dictionary-only regeneration revalidated 2,864 CDC/Census proposals, 1,567,292 record-scoped entries and 451,328 literal enumerations. CMS parsers additionally proposed 255 partial first-page rows across 13 records; 146 CMS records still have no parsed rows. Parsed documents also have unresolved pages, definitions, units and field-to-payload/release mappings. No dictionary was promoted into the eager Worker catalog.

Confirmed corrections include trusted generation and proposal provenance binding; whole-word metadata-label classification; full scientific-document identity selection; complete ACO suppression quotation; cached PDF/text integrity; explicit missing-capture outcomes; correct month ends; typed unknown filters and dictionaries; authenticated search/coverage pagination; equivalent generation-pin normalization; and oversized MCP frame isolation. Machine API observation grain remains unknown rather than inferred from unit tags.

The all-corpus inventory has 3,434 records. Discovery currently isolates four preexisting empty-description records and accepts 3,430; do not claim all 3,434 pass record validation. Proposed metadata gains do not certify scientific fitness. The 2022 ACO methodology linked to a 2024 distribution remains unresolved for applicability.

Ajv resolves to 8.20.0 using npm 11.19.1. Actual clean install, dependency-tree verification and 11 contract suites passed; npm advisory scan reports zero vulnerabilities. Sealed contract manifests remain unchanged. CI/gate bootstrap pin npm 11.19.1 and the minimum Node compatibility lane is 22.15.0. Exact Node 22.15 CI execution is not claimed.

No current complete-gate PASS is claimed. Full npm verification found stale WP0/WP11 approvals and a stale CI minimum-Node assertion. The CI implementation assertion is corrected and tested, but changed WP0/WP11/CI subjects require fresh draft evidence and owner authorization; historical approvals cannot transfer. AUTH-15 applicability, scientific limitations and latency acceptance are not implicitly accepted. The authoritative latest latency timestamp and outcomes are in FOLLOWUP-20260907.md, not inherited from the initial packet.

Outstanding engineering includes geometric CMS table extraction (the tested text-only relaxation recovered zero of 29 failed documents), bounded dictionary packaging/API integration, remaining API findings and full exact-candidate qualification. This document is progress evidence, not acceptance of missing information.
