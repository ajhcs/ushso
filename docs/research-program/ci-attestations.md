# PR-085 - Versioned CI checks and immutable historical approval

PR-085 records a read-only CI v1.4 structural inventory after the repository-wide subject changed. It preserves the complete CI v1.3 approval chain, represents the current code with a fresh technical subject, and does not issue a current approval, qualify a release, enable production behavior, or evaluate R01-R16.

## Candidate and ownership

The original PR-085 task dispatch is bound to `30fa0c59ecd4d3d1dd1f56cd8422c6c470e45cb0` (tree `8707c8fc92f35742f58508a83a57a3c4d9b7baf4`). The reviewed CI v1.4 continuation input was applied from `4ad67220df54334f70d891fd26d1778f83328d54` (tree `02544973bf065b3825c8b189f186a598da99a223`) under plan `f1b70e63ba9add91c8aa2f70d89b5c398167aa891566eddec68624b32e9b1f15` with independent review `d3c9c26c7566c74de2e5cec42c24c388a54e3cbe402f9ccf472ea05294f02b8f`; that continuation provenance remains explicit while the original dispatch base remains authoritative. The versioned inventory route was implemented in `3c884d3edcab1c75249508236693f9e82ded5dc3` (tree `c44be8b852df00af9021c9e3a16064f17461ea29`), followed by the reviewed PR-003 input-transition correction `d1826358b0b1e5f8db97b787b5bf74de5e50926b` (tree `5fec5290858bafddd6d5c4a291c36e5ea169ea0b`) and the atomic transition correction `6ed7fb4cb24199977034cf86bb3f0b4a928513be` (tree `959cdb237f1b733af839bd72747761d837e8ba04`). The final documentation commit is left for the root controller to bind externally because a commit cannot contain its own final SHA and tree.

Implementation authorship is native Luna Max (OpenAI; reasoning effort max), continuation task `ushso-pr085-ci-attestation-v14-20260910`, actor `luna-max`, on branch `codex/ushso-pr085-ci-attestation-20260910` and worktree `/mnt/d/worktrees/plumbob/.ushso-research-program-20260910-worktrees/ushso-pr085-ci-attestation-20260910`. Root owns independent review, final exact-head binding, integration, publication, and release gating.

## Immutable CI v1.3 proof

The historical CI v1.3 subject is `11b8c7269bf293e67690d713a9e9748b977fb3197f039cb9966e5647d05d2ccb`. The approval, evidence, and approved receipt remain byte-identical at the historical source commit `30fa0c59ecd4d3d1dd1f56cd8422c6c470e45cb0`:

- `verification/testing/ci/v1.3.0/approvals/approval.json`: 686 bytes, SHA-256 `315d0aa83399238b8e127ca3affae9e4d409fb7834e6c3d3aba6434b41b07ae0`;
- `verification/testing/ci/v1.3.0/approvals/evidence.txt`: 770 bytes, SHA-256 `a66da67cfee5c1dc4515aef9464afc222938ce27d3bc169da4393c211e8faa5e`;
- `verification/testing/ci/v1.3.0/receipts/approved.json`: 32,976 bytes, SHA-256 `8013e0b4700d781a365540e046cd6ac15c8eb1999aca956b30c73591ad648d4b`.

The v1.0 predecessor receipt remains 24,148 bytes with SHA-256 `6e27725cf531e1ced3d546503176af8ae4157366e97594c49ca5c9d2c18215cd`; the v1.2 predecessor-successor receipt remains 32,440 bytes with SHA-256 `d5f7b2d29cb6790a4b390f62a988746d8a9de6b36642c9d793179178f7d938e2`. `verification/research-program/ci-attestation/historical-ci-v1.3-pins.json` records these complete historical bindings and the separate reviewed PR-003 current-input pins. It is 6,914 bytes, SHA-256 `ed68d531b75e96173d1754c0d22caae976f3b65e80e760cc0940e3cd84440d9b`.

The old approved implementation and inventory inputs remain pinned. Current runner and package-lock drift is separately recorded for v1.4. Root `package.json` and `tests/contract-package-inventory.test.mjs` have two accepted states: their historical bytes, or the exact reviewed PR-003 bytes from `bf46d92b0e4fc91457bd3eb78aa1742ae84038f2` (package: 3,666 bytes, SHA-256 `b6c3469c7b10ecb90114199c18c3ebb293d801b8c8a3521cb25ded2fdba8d05e`; inventory test: 7,842 bytes, SHA-256 `5ab3deb8457c1a66fc313011bb0d96039c90f5ae35e1e08a9d34c9ab24d6c24b`). The current package is checked for unchanged metadata and registered commands, the exact extended root chain, and one research-program stage immediately after `test:worker`; the current inventory test is checked for its legacy-gate and aggregate assertions. Arbitrary drift is rejected.

## Current CI v1.4 draft

`verification/testing/ci/v1.4.0/` is a new versioned structural inventory. The current candidate verifier is `scripts/verify-ci-attestation.mjs` (SHA-256 `914ad3b3b0d65ff8ba901669d0081f099758442ddfcd328e71aa0d0222fc2b75`); the current runner is `scripts/run-contract-suites.mjs` (SHA-256 `9cc9efb8f29bf6a9ba93fe619e7fd1d0e519fce5cb0f37bf8747f199611cb647`). The v1.4 inventory, package and integration test hashes are `4e51bd9273e2febf8009c868ed8a41f34c9ff3cc3998141b6d8cca493f2bc8dd`, `f9fe5b00b2b471e6e60a48d8ed89af9d30c09cbe7ed5bc150e98fe0cec184bf7`, and `2cd934cc10d17c99c415994af11c73c29872a0059d4d9ab5a64dc7e8e0ad15ec`; the successor validator hash is `0413b0a140ec4e339199be1494d9146a7191e7e5d689b2cbeb5de68a8500bc41`. The focused root test file is `tests/ci-attestation.test.mjs`, SHA-256 `26e4e7f0187bbbccc4fd6c4d821a288403788278609cc337f401955f018a6f88`. The only new lock change is the npm 11.19.1 workspace link/descriptor; the current lock is 134,511 bytes, SHA-256 `37e4a9ec1fba9ab254aa7a596e1a0e3f73919cf5358717dc6e06e0478be6b20c`.

The read-only replay is `verification/research-program/ci-attestation/current-ci-v1.4-replay.json`, 11,054 bytes, SHA-256 `8449f912452cfc828cbee1b72605634f298304220c0600adbb8359b906987cf4`. It reports current subject `1530562670b67d96977be8e760ac13a873cbe3aa284145d6fe1d3b878ad8a4fc`, technical status `PASS`, status `pending_authorized_review`, approval `null`, and all release/production flags `false`. The historical subject remains separate and is not transferred.

The aggregate route selects only `ci-verification` at `verification/testing/ci/v1.4.0` with `node tools/validate-package.mjs --validate`. Its test stage executes normally before the current verifier. Direct v1.4 `--validate` and `--issue` remain strict and returned pending without writing a receipt. Future versions use the normal strict path. Child nonzero, error, timeout, and zero-test outcomes remain failures. Both the original root chain and the exact PR-003 extended chain are accepted only with every legacy gate and exactly one navigator aggregate.

## Verification evidence

The v1.4 package integration suite passed five tests in `cmd-0025-1bb26230`. The prior focused runner suite passed nine tests in `cmd-0026-246ee7e0`; the corrected direct suite passed ten tests in `cmd-0039-f2390da5`, including the real `git show` PR-003 input transition, historical legacy control, one-byte transition tamper rejection, both mixed historical/reviewed input-pair negatives, exact route, strict direct paths, and real subprocess failure cases. `v1.4-runner-negative-fixtures.json` records the subprocess cases and is 2,266 bytes, SHA-256 `ce48082de26c4bd619163b74e9ac1200c392d3c212ad984e507f1d74a993a75e`; `v1.4-input-transition-negatives.json` records the two actual mixed-pair failures and is 2,385 bytes, SHA-256 `6fefb7b4d2947c6faa9c9cc5da90f0f48b0a604728e6a2a30b3ec41588a966b5`. The preserved WP0 component replay passed all ten prior focused tests; its external root receipt remains the authority for that immutable component.

The npm 11.19.1 locked install passed (`cmd-0014-35c5664e`, `npm ci --ignore-scripts --offline --no-audit --no-fund`). The direct producer build passed in `cmd-0023-f00f3acb` from `2026-09-11T02:10:21.576Z` to `2026-09-11T02:11:08.245Z` (46,669 ms, exit 0). The build plus artifact-only Wrangler dry-run passed in `cmd-0024-911db093` from `2026-09-11T02:11:35.610Z` to `2026-09-11T02:12:39.493Z` (63,883 ms, exit 0); no deploy or network/provider request was made.

The full npm test was run directly in `cmd-0022-aa4a557b` from `2026-09-11T02:04:05.057Z` to `2026-09-11T02:09:54.560Z` (349,503 ms, exit 1). It retained `verification/wp11/v1.3.0:validate` with `SUCCESSOR_APPROVAL_STALE_SUBJECT`; stdout was 215,642 bytes with original SHA-256 `bd7d7b42daf4f196c0a3b86c45402ad86b11850604a3d726c82559598e49c603`, and stderr was 259 bytes with SHA-256 `a68e7527aaf915307bdba5bde9a67c150ef044dd810d270432d6e79377e3bb92`. This failure is preserved as a release block; no historical approval was repinned. The earlier retained CI failure metadata and controller run remain unchanged in the evidence index.

Sanitized command metadata is in `verification/research-program/ci-attestation/v1.4-command-receipts.json`, 22,872 bytes, SHA-256 `fb8c51c68fea8b2d6c3eeedec0d29eed85a658e5afd456c659c5b40227ff82ce`; the lock/install artifact is 1,227 bytes, SHA-256 `a55a59017b89f15cf06ce41f52c946c429bc88a6519c2da45bef1dcf1cdbea55`. The prior failed CI metadata is retained and no private evaluator input or provider/network call was used.

## Handoff and release boundary

`verification/research-program/pr-085/evidence-index.json` binds the continuation base, implementation/correction commits, current hashes, historical chain, reviewed PR-003 transition, command receipts, and release boundaries. `docs/research-program/handoffs/PR-085.json` leaves its final head null for root to bind after the documentation commit and independent review. The handoff is authored by native Luna Max; it does not constitute independent acceptance.

PR-082 must obtain current-subject successor authorization and independent release qualification. A development technical pass, historical approval, or this handoff cannot satisfy that requirement. PR-003 and PR-004 interfaces remain separate. Every R01-R16 predicate remains unaccepted; this work records CI and attestation evidence only.

## WP11 v1.3 current-attestation continuation

Native PR-085 ownership could not resume (agent thread limit reached). This Grok continuation implements the reviewed WP11 current-attestation boundary from prepared source `30dc908c27269710763241887dcd5b6215196668` (tree `1db855220a66bf973d290426eab74047cbd5433f`) without replacing the original dispatch base `30fa0c59ecd4d3d1dd1f56cd8422c6c470e45cb0` (tree `8707c8fc92f35742f58508a83a57a3c4d9b7baf4`). Plan SHA-256 remains `176fe62ddb6ed9e574e84d7f8d0cf7d06344ec40e3ba13a2cd41d3711f17a49d`; accepted scope receipt SHA-256 `bbae964e9481ee43a06184f208f4ed0125b7fccb22f1ca31c4398a51009177dc`; independent review SHA-256 `03ebb668ccdc75becb8818d67d3169e40d1f2d660c1f769e1df424cf20c9fe48` with disposition `pass_with_implementation_conditions`. Functional implementation is `778b968f50e7b7e2e93c503274ca3a590bf63e39` (tree `ff6d1a4059265b7071e6c4a8a2182e6c6042ba41`).

The adapter reuses the existing WP11 v1.3 technical builder and validator. Historical approval remains bound to subject `294d8b40bb5a2dbe1f55cdfde4a60205de75ee1ffe48e0108eae69aea2db0f98`. The current technical draft subject is `8d3409bc360d9b295c5c21f9b1a2d6ad91f42ff5c946a14aff92cd4b7d97c259`, status `pending_authorized_review`, approval `null`, and all release/production flags `false`. The named wrapper subject `@ushso/wp11-current-attestation-adapter-v1.3.0@1.3.0` is `0e55f850f52b292b1172223abc7eb9980eb1c10a739af806a3bf55571415957c` and is distinct from both the historical and current WP11 technical subjects. Historical approval is not applied to either current subject. Direct `node tools/verify.mjs --validate` still exits 1 with `SUCCESSOR_APPROVAL_STALE_SUBJECT`.

The aggregate route matches only alias `wp11`, path `verification/wp11/v1.3.0`, version `1.3.0`, package `@ushso/wp11-verification-v1.3.0@1.3.0`, and `node tools/verify.mjs --validate`. The real four-test WP11 suite still executes first. Future versions, wrong package names, altered validate commands, and nonmatching paths keep ordinary strict execution. Child nonzero, signaled, timed-out, and zero-test results remain failures. No workspace, package.json, or package-lock change was made.

PR085 original `package.json` is recorded as an external Git snapshot at `4f90157108a92ce9541c340d5536eac31f24a1d8:package.json` (3,555 bytes, SHA-256 `25874c7d9464210794bd7a1467b117ef5fc71808fdf5e34726ea65181744561c`). The reviewed PR003 package transition remains separately named from `bf46d92b0e4fc91457bd3eb78aa1742ae84038f2` (3,666 bytes, SHA-256 `b6c3469c7b10ecb90114199c18c3ebb293d801b8c8a3521cb25ded2fdba8d05e`). The producer snapshot is not relabeled as the combined manifest. Prior command receipts and `verification/research-program/pr-085/evidence-index.json` remain byte-identical; this continuation adds `verification/research-program/pr-085/wp11-continuation-evidence-index.json`.

The selected-package static audit is complete. Addendum SHA-256 `208c78a34bb7b7626d5336b279bd137ce127e47a077b9c1f8bba8006b88a1f2a`; correction receipt SHA-256 `8c15e44e4115a336d7a7db7ae7bbd5c966a3a27dfb2094c3829058f39e692f02`. Known WP0/WP11 lock drift and two program-ledger raw-pin findings are recorded; they do not authorize historical program-evidence edits. The separate PR003 fixture/live-ledger coupling failure remains outside this WP11 scope. Full combined npm test/build/cf:dry-run/hosted CI remain root's combined gate. No current approval, merge, deployment, or R01-R16 acceptance is claimed.

### WP11 test context correction

Root Astra corrected two test assertions after the combined PR003 manifest exposed assumptions about the original PR085 manifest. A separate Grok reviewer approved the exact two-test candidate; the runtime verifier, runner, policy, package manifests and historical proof are unchanged. The historical positive control explicitly injects original Git snapshot bytes. The default test checks the exact recognized manifest present. Current wrapper evidence changes because it binds the test file. The new focused nine-test replay and current verifier are recorded in `verification/research-program/pr-085/wp11-test-context/`; previous producer receipts and indexes retain their original subjects. Full combined qualification remains pending.

### CI test context correction

The first full gate on combined candidate `c70b0790ca439d56bb2c9936d53837c6f68b6d57` failed two CI tests that assumed the original root chain (343 of 345 worker tests passed). Root Astra corrected those two existing test bodies after separate Grok review: the historical positive control supplies original Git bytes, and the inventory assertion checks the exact reviewed manifest present. Runtime validators, inventory builders, routing, policies, manifests and historical proof remain unchanged. Both actual manifest contexts passed all 15 focused tests before application. New committed-command evidence and the newly pending CI subject are under `verification/research-program/pr-085/ci-test-context/`; older receipts and indexes retain their original subjects. The failed full-gate result remains evidence, and a changed combined gate and fresh hosted CI are required.

## PR-086 - Separate original WP11 input proof from current verification

PR-086 retains the original 154 WP11 v1.3 input preimages (1,347,732 bytes) from `30fa0c59ecd4d3d1dd1f56cd8422c6c470e45cb0` in a portable content-addressed snapshot under `verification/research-program/ci-attestation/wp11-v1.3.0/historical-preimage-snapshot/`. The snapshot matches sealed receipt `e596e1b18a0251f611990c9752e1d12fb36cd05dab16bf55cf96e8d1fc431f9d` and historical subject `294d8b40bb5a2dbe1f55cdfde4a60205de75ee1ffe48e0108eae69aea2db0f98`. Historical proof reads those retained bytes through `historical-preimage-reader.mjs` and does not require Git or network. Retained sources are evidence only and are not executed.

The existing WP11 v1.3 technical builder, validator, successor helper, and general runner are unchanged. Current technical evidence is built from actual current files and remains `pending_authorized_review` with `approval=null` (subject `7e355d27a6deb558b6626c486472763a7cfd61239e054d16e13256162de3829f` on this producer head). The wrapper subject `@ushso/wp11-current-attestation-adapter-v1.3.0@1.3.0` binds the adapter, tests, policy, unchanged runner, snapshot inventory, and snapshot reader; it is distinct from both the historical and current technical subjects and also has `approval=null` (subject `87fb320c9f415322f52581f3416ac760a58da684bdafd4592bac909bf182115a` on this producer head). Direct `node tools/verify.mjs --validate` and `--issue` still exit 1 with `SUCCESSOR_APPROVAL_STALE_SUBJECT` and write no receipts.

Current-versus-historical reporting lists every changed sealed input. This integration tree has the two named package transitions (`package.json` PR-003, `package-lock.json` PR-085 CI v1.4). Original PR-005 `941c9cd02a3a87c4239e6b75cdccbf4c8ec095e9` reports 13 changed inputs (11 product files plus those two transitions) and is not combined acceptance. Corrected PR-005 `afb90565456a429e73527f2bb99daf9cf174aa3c` is a supplemental unapproved comparison only. Focused tests keep that named pre-PR005 two-change fixture and independently compare actual current bytes against every original sealed pin (path, hash, and length). They do not treat this two-change tree as every future current tree, do not weaken to a first-failure or `>=2` check, and do not repin history. Current-file mutation runs the actual builder/validator against a task-owned TMPDIR fixture so tracked source is never temporarily rewritten; historical hashes come from the retained snapshot and before/after hashes come from the real current files. The existing builder still reads `worker/index.mjs`, `packages/coverage/accounting/v1.0.0/artifacts/public-coverage-view.json`, `verification/external-authorization/v1.0.0/register.json`, and `packages/retrieval/corpus/records.jsonl` without listing them in the 154-file inventory; that coverage limitation is recorded and is not expanded here.

Prior PR-085 `historical-pins.json` and `current-replay.json` remain byte-identical prior-candidate evidence. Additive PR-086 replay and command receipts are `pr086-current-replay.json` and `pr086-command-receipts.json`; the r2 test correction adds `pr086-r1-current-replay.json` and `pr086-r1-command-receipts.json` without rewriting those prior files. Current technical subject remains `7e355d27a6deb558b6626c486472763a7cfd61239e054d16e13256162de3829f`. The wrapper subject is now `822c69777c46a237eefb7da3e299854b1562ec1ac726ef1c9fa37add2c2530eb` because the adapter and tests changed; both remain unapproved. Producer checks: 17 focused tests, current adapter, selected WP11 aggregate, and strict direct validate/issue. Root independently reviews original byte hashes, binds the final head, and runs the combined gate/CI. No historical approval is widened, no current approval is issued, and PR-082 still owns current-subject approval and release qualification.

