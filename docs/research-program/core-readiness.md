# Core-cohort research readiness

Policy version: `ushso.core-readiness.v1`.

The frozen 100-product cohort is the denominator. Annual releases of the same product are not additional products. Missing essential context remains a named blocker.

Public-sample completion and restricted/manual route completion are counted separately. Catalog membership is not a payload sample. Current calculated payload-sample complete is receipt-bound `bounded_sample=true` only. A thousand populated optional fields cannot compensate for an unknown essential denominator or inaccessible recipe.

Every incomplete priority product has a concrete next task. The program gate cannot waive it by adding more easy records. R04/R05 remain unaccepted.

Sealed `packages/coverage/accounting` artifacts and frozen `evaluation/research-program/cohorts.json` are not edited. Last-good generation `live-2026-09-03-85b50522b420` is unchanged.
