# Hospital price-transparency MRF discovery

Policy version: `ushso.hospital-mrf-registry.v1`.

The frozen 25 hospital candidate IDs from `evaluation/research-program/cohorts.json` are consumed without replacement, silent deduplication, or shrinking the denominator. A health-system landing page is not counted as a verified file for every hospital it owns.

Broken links, redirect gates and unexpected formats receive typed outcomes. No crawler explores arbitrary sites. Declared and detected schema versions remain distinct. An enforcement dataset cannot satisfy a rate-file entry. No full-size MRF download is initiated without its own budget decision.

Sealed `packages/identity` and frozen `cohorts.json` are not edited. Last-good generation remains `live-2026-09-03-85b50522b420`.
