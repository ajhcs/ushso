# Research package integration

This candidate exposes publisher-derived dictionary proposals for 2,905 catalog records, containing 1,573,847 proposed fields. It also exposes four qualified scientific notes and 90 unresolved conflict rows. The package remains a research-review surface: canonical catalog facts are unchanged, schema applicability is unresolved, and publication_authorized remains false. Labels, definitions, and scientific conclusions must not be treated as interchangeable.

The archive and its package graphs are pinned in config/research-assets.lock.json. The dictionary manifest is 0f10c6b764d054cd15324c4a1d93d6da191d86b792976d9573cdf27feceecb6b. Hash-checked composition preserves parser issues and superseded extraction attempts. It checks independent expected sources by resolved filesystem path, and the archive builder rejects changing inventories, unsafe filenames, and symlinked source/cache boundaries.

Oversized fields retain exact reconstructable values. Short display definitions are compared with the reconstructed original; omitted previews are not described as missing publisher definitions. The HTTP release smoke traverses all 136 newly qualified glyph rows and verifies pending-review boundaries for notes and conflicts.

Rejected request bodies are consumed or cancelled within byte and time limits before returning their existing typed HTTP errors. Stream failures cannot replace those intended rejection responses. Successful JSON reads are also byte- and time-bounded.

Before production, require the complete exact-candidate gate, retained artifact hashes, a shared cursor signing key, and post-upload HTTP/native-client verification. Scientific approval, a guaranteed sub-300-ms cold response, and independent held-out scientific evaluation are not conferred by a technical gate. Historical attestations stay immutable; new technical attestations use explicit reviewer identities and exact subjects.
