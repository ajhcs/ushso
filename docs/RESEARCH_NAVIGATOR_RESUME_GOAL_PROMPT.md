# Resume goal prompt for Luna Max + Co-Engineer

Copy the prompt below into a new Codex goal on the Research Navigator PR branch.

---

/goal Resume and complete the USHSO Research Navigator implementation from the open handoff PR and bring `docs/RESEARCH_NAVIGATOR_IMPLEMENTATION_PLAN.md` to its full Definition of Done.

You are Luna Max acting as the primary implementation and verification agent, with Co-Engineer available for bounded parallel assignments. Treat the implementation plan as the authoritative specification and `docs/RESEARCH_NAVIGATOR_HANDOFF_2026-08-31.md` as the current evidence-backed checkpoint. Begin by reading the repository `AGENTS.md`, the relevant production-host notes under `/home/plumbob/.codex/memories/plumbob-server/`, the implementation plan, the handoff, the existing ADRs, and the external-authorization register. Inspect the entire PR diff and current repository state before modifying architecture or trusting any generated receipt.

Preserve the product boundary throughout: USHSO recommends, explains, and compiles deterministic evidence-bound research plans from public-source metadata. It does not execute analyses, calculate HHI or market share, create financial benchmarks, download restricted datasets, automate credential/payment/application flows, or become a general analytics product.

The branch contains a large, intentionally provisional implementation spanning WP0–WP14. Do not restart it, flatten its versioned contracts, or treat checked-in receipts as automatically current. Preserve unrelated user work. Do not deploy, push additional remote changes beyond this PR branch, incur paid infrastructure, contact live source systems, apply production/staging migrations, change secrets, cut over traffic, retire legacy storage, or perform irreversible external actions without explicit authorization. Record every external boundary precisely and complete everything possible locally.

Use Co-Engineer for genuinely independent, bounded reviews or implementation slices, but keep dependency ownership explicit and integrate only reviewed work. Maintain one tracked execution plan and an evidence ledger mapping every implementation-plan requirement and tester paragraph to code, migration, tests, receipts, documentation, and authorization state. Do not let parallel agents reseal shared manifests while their inputs are still changing.

Start with a checkpoint audit:

1. Confirm the PR branch, merge-base, worktree status, and upstream delta. Review every provisional area left by interrupted agents: `db`, `services`, `packages/connectors`, `verification/testing`, `verification/wp13`, and `verification/wp14`.
2. Verify that `.serena/` or other machine-local state was not committed. Preserve `RELEASE_PROVENANCE.json` as historical production evidence; do not reinterpret it as authorization for this program.
3. Treat all package manifests, fingerprints, validation reports, and WP receipts as stale until their exact input set is frozen and recomputed.
4. Reproduce the handoff's known green and red commands before expanding scope. Resolve failures rather than waiving them.

Then continue in strict dependency order:

1. Finish WP3–WP4 persistence and ingestion. Review migrations `0001`–`0003`, role/bootstrap and least-privilege controls, scheduler/harvest worker composition, Hyperdrive usage, Workflow step boundaries, event/outbox idempotency, retry budgets, DLQ handling, replay safety, leases, observability, privacy, retention, archive/restore, and rollback. Before binding any local service, run `ss -tlnp`. Use a fresh `pg.Client` per operation or Workflow step from `env.HYPERDRIVE.connectionString`, parameterize queries, and close clients in `finally`. Run clean-install and N-1 upgrade migrations plus failure-injection and concurrency tests.
2. Finish the ingestion v1.1 contract and WP5 connector waves. DataCite requires bounded query keys such as `page[number]` and `page[size]`; allow only balanced, single-level bracket segments and adversarially reject empty, unbalanced, nested, control-character, and oversized names. Support v1.1 descriptors alongside truthfully versioned v1.0 harvest/capture/checkpoint envelopes through object-aware validator dispatch; reject unknown versions and downgrade ambiguity. Complete all offline delivery-wave fixtures and parity checks. Keep every live connector disabled or fixture-only pending authorization.
3. Freeze WP4 migrations/contracts, then finish WP6 migrations `0004`–`0007` and canonical normalization. Preserve append-only history and immutable prior facts; represent supersession as versioned edges rather than mutating prior rows. Verify source identity, reference closure, deterministic snapshot materialization, N+1 updates, concurrent ingestion, restore, reconciliation, and review-queue behavior. Uncertain identity/dedup/family matches must remain reviewable and must never be silently merged.
4. Integrate WP7 identity/family/version grouping, WP8 immutable production search generations, and WP9 public coverage accounting against the canonical database. Prove generation pinning, atomic activation, rollback, garbage-collection dependency safety, audit pinning, and coverage denominator invariants. Keep WP8 tuning untuned until the protected held-out authorization and keep public wording provisional until sign-off.
5. Complete WP10–WP12. WP10A Use Cards and Access Plans must remain source/evidence bound. Do not implement or activate WP10B planner behavior until planner-governance authorization; once authorized, the compiler must be deterministic, bounded, citation-complete, non-executing, and clarification-token safe. Finish required WP11 researcher and expert-review evidence. Keep WP12 WebMCP/JSON tools bounded, schema-identical with the UI/search truth boundary, protected, and unwired until cutover authorization.
6. Independently re-audit WP13. Re-run artifact/document/render/sitemap tamper tests, deterministic rebuild checks, canonical-origin binding, content negotiation and `Vary: Accept`, repeated-decoding secret-path rejection, public-locator/redaction provenance, rollback/static-retention integrity, and independent DCAT 3 conformance. Reseal only the exact reviewed candidate.
7. Independently re-audit WP14. Prove candidate-to-active promotion, rollback to the prior generation, durable CAS transitions, append-only transition ledger, non-replayable cycles, trusted time handling, authorization/gate/telemetry evidence, full rollback bundles, soak and retirement policy, strict schemas, and adversarial concurrency. Do not accept self-attested authorization or no-op rollback receipts.

Resolve the legacy retrieval validation issue explicitly. The historical v1.0.1 manifest pins `observatory/index/v1.0.0/schemas/observatory-record.schema.json` at SHA-256 `2d778a3125ba03c7504aad92e7154fdedf686db66619e439a6259ba883e162d9`, but those exact bytes were not committed or recovered. Never substitute a looser schema while claiming the old digest. Recover the exact bytes if possible; otherwise write an ADR and produce an explicitly versioned compatibility successor with its own strict schema, provenance, tests, and receipt while preserving the historical pin and limitation.

After all workspace descriptors freeze, regenerate `package-lock.json`, run the full contract/package inventory, and reseal in dependency order. The prior run observed `npm run test:worker` at 40/41 with only missing `verification/testing/*` workspace links, but assume additional drift until proven otherwise. Complete the seven testing-strategy packages and then run all required unit, contract, connector-fixture, migration, upgrade, failure-injection, concurrency, retrieval, planner, security, performance, generation-pinning, rollback, coverage-invariant, and product-boundary gates.

Maintain the external-authorization boundary. The current register leaves paid infrastructure, remote state/secrets, staging apply, live connectors, managed recovery drills, internal canary, public cutover, production soak, JSONL retirement, remote Git push, production zero-traffic foundation, planner governance, protected retrieval holdout, identity adjudication, and coverage wording authorization as not requested/not authorized. Add or explicitly cross-reference the WP11 researcher usability and expert asset/use-card studies. Do not manufacture PASS receipts for any human or external action.

At final freeze, update every bound manifest and receipt exactly once in dependency order; update the program evidence ledger; run a standard repository security scan; run the complete performance, migration, rollback, and coverage gates; and execute the repository release gate exactly once against the frozen candidate. Resolve failures instead of merely reporting them. Do not claim completion while any plan requirement, acceptance criterion, migration, rollback capability, quality gate, documentation obligation, authorization, human study, or Definition-of-Done item remains incomplete.

The final handoff must include implemented capabilities; all changed files and migrations; exact verification commands and results; contract, coverage, evaluation, security, performance, migration, generation, and rollback receipts; remaining external authorization steps; safe deployment and rollback instructions; and a requirement-by-requirement demonstration of the implementation plan's full Definition of Done.

---
