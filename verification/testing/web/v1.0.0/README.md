# UI and crawler test-strategy evidence v1.0.0

This private package maps every §22.6 bullet to exact local UI and crawler evidence. The candidate's deterministic no-JavaScript, sitemap, structured-data, alias, and withdrawal tests do not substitute for the timed researcher study, authorized canary/public activation, or a live crawler smoke. Direct scripts are offline and read-only.

```sh
npm test --prefix verification/testing/web/v1.0.0
npm run validate --prefix verification/testing/web/v1.0.0
```
