# Metadata-check receipts (Track 4)

No live HTTP has been executed. No receipts yet.

After `AUTH-METADATA-CHECK` is granted with a rebind to the exact execution HEAD plus a validity window, `scripts/research-program/run-metadata-check.mjs --execute` will write SHA-bound receipts here (in git) for gitignored captures under `verification/research-program/evidence/payloads/metadata-check/`.

Ledger: `verification/research-program/evidence/metadata-check-ledger.json` (separate from payload). Packet: `verification/research-program/evidence/metadata-check-packet.json`.
