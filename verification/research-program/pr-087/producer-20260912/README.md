# PR087 producer evidence

Source H087 `33b3146dec73984087b49724cc18e690310b0b1d` (tree `e130179e17b0d4b32aa6ad7b01cfb1e56f1698e9`) contains the complete sixteen-file current WP5 package and exact two-entry lock addition. The later C0873 commit transports evidence only; its Git identity is recorded externally after creation. This packet does not accept research integration, issue approval, waive R requirements or enable production.

Final producer checks: 23 focused groups on Node22.15 and Node24.14, all48 actual connector leaves, full145-assertion validator and fresh matrix/delivery/parity/reconciliation controls. Complete current subjects have290 repository files,140 loaded modules and4 package boundaries. The actual Node runtime contributes to identity; generated results remain outside the sealed package.

`lock-review-input.json` identifies the exact immutable correction input. The unchanged current WP11 adapter rejects its unreviewed lock. The frozen WP11 direct stale-subject and frozen WP5 stale-fingerprint rejections are retained separately. Root owns independent exact-source/lock review, PR086 continuation, joint gate, hosted CI and any publication/integration.

`prior-attempts.tar.gz` contains only the explicitly listed command/probe subset in its manifest. Caches and dependency trees are excluded. All initial failures and the previously reviewed candidate identities remain distinguishable; see history-notes.json. Event receipts retain original raw command records, stdout and stderr. Earlier runtime commands bind their exact bytes to H087 via source-manifest.json; completion bounds based on original receipt filesystem mtime are labeled.
