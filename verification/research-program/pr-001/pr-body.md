# PR-001 — Reconcile the deployed release and integration base

The selected release, exact integration base, dirty original workspace, historical work, and pending research assets needed one digest-bound subject before downstream implementation. This PR records that subject and a bounded legacy crosswalk with replayable local checks; it does not change Worker behavior or public runtime behavior.

- Assignment: PR-001; phase P1/sub-phase 1A; requirements R01 and R16; finding F01.
- Integration base and dependency merge SHAs: base `45210704b8de2d7b1360b6d32657cd17791bdd77` (tree `35de194e7182a67a0275cc180f2b021da184aacd`); dependencies none.
- Exact candidate head: the final C-001-3 commit is supplied to the controller out of band after commit creation; its parent is `1b572930ce010373de2e19dabb5faf27cfef5c69`.
- Source/corpus/schema/model identities affected: same-tree source candidate `11b268c17b5b65261041734abca456720b0d9706`; canonical corpus `ushso-live-catalog-2026-09-03` v1.2.0 with 3,434 records, 3,430 searchable records, and four isolated records; public contract v1.3.0; candidate SEMP model `MODEL-USHSO-SEMP-001` v0.5.1 remains proposed/not approved; the locked dictionary archive and scientific review packages remain pending review.
- User-visible result, including changed uncertainty/access behavior: no runtime behavior changes. The published baseline distinguishes the metadata-only canonical corpus from publicly packaged dictionary proposal assets served through the existing review route; proposal publication remains unauthorized for canonical scientific promotion, and unresolved access, freshness, schema applicability, and Worker-byte claims stay typed as unresolved.
- Verification: `python3 verification/research-program/pr-001/verify.py` with the explicit retained live-capture, deployment-readback, preservation-manifest, audit CSV, proposal-manifest, and original-workspace paths passed all identity, byte, count, authorization, and runtime-path checks (exit 0; `cmd-0019-b743735d`). Crosswalk JSON, boundary-destination, source-hash, authorization-expiry, and SEMP-scope checks passed (exit 0; `cmd-0023-8317d220` through `cmd-0027-ce94155d`).
- Handoff and sanitized evidence links: `docs/research-program/handoffs/PR-001.json`, `verification/research-program/pr-001/baseline-identity.json`, `verification/research-program/pr-001/crosswalk-evidence.json`, and `verification/research-program/pr-001/workflow-evidence.json`; local retained captures and manifests are identified by SHA256 in those files.
- Failed/skipped checks and remaining limitations: early bounded parser/shell attempts and an initial verifier HEAD assumption failed and were corrected; the corrected receipts are retained. Full `npm test`, `npm run build`, and `npm run cf:dry-run` belong to the integration/release gate and were skipped for this documentation/evidence-only scope. Worker binary byte identity, proposal/scientific schema applicability, freshness, AP01 principal binding, source payload access, independent review, and production cutover remain unresolved or gated.
- Rollback or compatibility impact: no production mutation, deployment, source retrieval, provider call, approval receipt, or generated SEMP view changed; revert the three PR-001 commits to remove the documentation and evidence packet without runtime rollback.
- Review requested from Astra; named owner/domain decision if needed: Astra should inspect the final three-commit head and replay the decisive checks. Scientific approval, authorization, deployment, and later reuse decisions remain with their named owners.

Do not claim deployment, source access, scientific approval or independent review from a producer test. This PR records exact evidence and preserves the stated uncertainty for downstream review.
