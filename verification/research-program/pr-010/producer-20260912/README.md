PR-010 producer evidence for immutable source 1318cda5c76cd3262c7668fb7f56fab842876c6f.

41/41 lifecycle, 11/11 research/update, 46/46 ingestion and 48/48 connector tests pass. The connector suite includes eight portable actual-module cursor/identity/304 regressions. All 36 adopted behavioral groups have producer evidence; this packet grants no independent approval.

The actual refresh.capture entry point consumes the paired explicit bridge and deterministic observation clock. Public capture-reference locators omit query/userinfo/fragment; full request/capture identity and compiled/final coherence remain query-sensitive. The serializer bytes equal the exact adopted candidate. Released validators, the global allowlist, WP5 v1.0, SQL, service compositions and the root lockfile are unchanged.

The journal serializes whole transactions and poisons all clients on persistence ambiguity. One scheduler parent can own separate child repositories. Fresh-process tests retain the same attempt, request charges, capture identity and checkpoint lineage across real SIGKILL windows. An unknown delivery never automatically refetches. Unexpected empty-enumeration checkpoint refusal is retained as a durable safe internal_failure with prior captures/pages; it cannot become success.

Initial producer failures and later corrections are retained in history-index.json. Root's independent capture/admission corrections and the original cursor incompatibility remain in their controller adoption capsule and external task scratch. No initial failure is represented as a successful run.

Independent review, the exact PR087 current-technical successor and PR086 lock continuation adoption, a combined full local gate and hosted CI remain pending. C-009-1 measured topology qualification remains unresolved. No R01-R16 acceptance, real-source coverage, release qualification or production activation is claimed.
