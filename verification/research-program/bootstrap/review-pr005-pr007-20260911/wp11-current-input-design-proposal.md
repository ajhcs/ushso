# Proposed bounded WP11 current-input correction — review before implementation

This is a controller-authored design proposal, not an approved policy change. Source integration head: 0b4d699be77eaf7b57105fe6633033aec2e73062. Trigger: PR-005 head 941c9cd02a3a87c4239e6b75cdccbf4c8ec095e9, independent WP11 suite failure and complete 154-input diagnosis in wp11-pr005-current-input-diagnosis.json.

The current adapter validates immutable WP11 v1.3 approval/evidence/receipt/predecessors, then requires nearly every current implementation input to equal the old approved inventory. PR-005 legitimately changes 11 of those inputs. Its proposed one-file repin is incomplete and would not establish new approval.

Review whether the following is a sound bounded follow-up, or return a more precise safe design with its limitations:

1. Keep the existing historical WP11 package, approvals, evidence, receipts and predecessor proof byte-for-byte. Retain a portable content-addressed snapshot of all 154 actual historical input files matching the existing sealed receipt, with exact source commit/path, size and SHA-256. Validate every byte against the immutable approved inventory. Prefer retained bytes that work in shallow/offline CI; do not substitute current files or synthetic historical bytes. If any original byte is unavailable, report that exact missing input.
2. Separate historical input verification from current input attestation. Continue invoking the unchanged actual WP11 technical builder and validator on current source bytes. Bind the full current inventory and all adapter/policy/runner/test/snapshot-reader inputs into a separately versioned current subject. Preserve every technical, disabled-feature and no-approval assertion. Current evidence remains a pending draft; development validation must not transfer historical approval to changed inputs or qualify a release. There must be an explicit reviewable changed-input report, not a blanket silently approved drift list.
3. Retain exact existing aggregate routing, actual WP11 test execution, strict direct successor --validate/--issue behavior, future-version handling, zero-test rejection and failure/timeout/output-bound propagation. Add positive and negative tests for complete historical proof, each tampered/missing input class, changed current source producing a different pending subject, stale current pins, historical substitution, incomplete inventories and approval overclaims. Verify original/combined candidates separately; old gate receipts do not qualify new code.

Potential owned scope: scripts/verify-wp11-attestation.mjs, tests/wp11-attestation.test.mjs, verification/research-program/ci-attestation/wp11-v1.3.0/, docs/research-program/ci-attestations.md, plus a new follow-up task's own handoff/evidence. Do not edit the general runner unless review identifies an exact necessary change; current routing already calls the adapter. No root manifest/lock, other verification package, historical WP11 source, frozen cohorts, deployment or successor helper changes.

The follow-up should be a new stable bounded PR with three planned atomic commits and accepted PR-085/PR-003 prerequisites. Release qualification PR-082 must consume it. PR-005 functional corrections can proceed independently, but cannot be accepted into integration until the combined current-input verification passes. PR-085's existing acceptance is not relabelled as approval of this new scope.

Please inspect the real adapter, builder, validator, historical subject construction, existing tests and stored evidence. Determine whether the historical snapshot can be validated without weakening scientific or execution gates, identify any exact additional scope required, and give a concrete verdict. Do not implement product or policy changes during this review.
