# Capture-reference public locator correction — scratch prototype

The minimal connector correction passes **8/8 offline cases**. It clears query, username, password and fragment only when serializing `capture_reference.source_locator.redacted_locator`. The unchanged global `redactedLocator` still supplies query-sensitive request coherence and deterministic observation identity. `capture-protocol.candidate.mjs` is the proposed exact source; `capture-protocol.proposed.diff` shows the narrow change. Nothing was applied to a repository.

The original two-page capture fails the frozen ingestion v1.0 validator with `CAPTURE_LOCATOR_SECRET_RISK` on `?cursor=page-2`; that actual failed record is retained in `observations.json`. The candidate completes the real two-page runner with strict capture/checkpoint validation. Equal body and clocks at different approved cursors retain distinct original capture IDs and request IDs. Wrong final query still fails before writes; no-query output is identical; tampered public query is still rejected. Two-page 304 reuse keeps the original references and provenance, with no new capture commits or response bytes.

304 matching uses the canonical full request key and capture ID, not the public locator. Query-free public locators alone cannot distinguish pages and must not be promoted into identity keys. Invalid historical query-bearing references remain invalid; this change does not migrate or overwrite them. Replaying the same identity against different old reference bytes retains the existing idempotency-conflict behavior.

Controller adoption must explicitly own `packages/connectors/src/capture-protocol.mjs` and meaningful additions to `packages/connectors/tests/contracts.test.mjs`, and resolve the known frozen WP5 fingerprint successor before claiming a passing joint gate. The producer should translate the retained prototype cases into those actual contract regressions and rerun them against its immutable integrated implementation. All released validators, allowlists, global locator behavior and activation boundaries remain preserved.

The executable scratch module explicitly rebinds its three imports to pinned baseline source at integration `50a844d218600f95c13e64c2db64ab4d8562ae0d`; the proposed source keeps original relative imports. The test injects that prototype protocol through the real client/runner while the actual unchanged validator checks reference commits. All 62 inspected runtime/schema/lock pins matched before and after. No external fetch occurred; transport and DNS ports are the existing fixture implementations.

An initial test-fixture omission caused two checkpoint errors because the synthetic records lacked required publisher-modified timestamps. Both failed outputs, the original fixture and the correction are retained. The source candidate was unchanged throughout. `command-receipt.json` binds the final 8/8 execution; no full gate, source job, publication or final implementation acceptance occurred.
