# Corrected PR010 proposal review

**One P2 correction remains before adoption.** The proposed `data.cdc.gov/__ushso_fixture__/catalog.json` locator passes the legacy research host check but cannot pass the frozen connector validator. `source-route-allowlist.mjs:1,276,307` admits only exact fixture ID `source_fixture_catalog`, existing fixture hosts `catalog.example.gov` / `mirror.example.gov`, and existing route tuples such as `/data.json`. Other `source_fixture_*` IDs have no positive allowlist. `route-manifest.mjs:248` enforces this during descriptor validation. The proposed A1/C1 positive path therefore cannot execute within the stated preserved connector scope.

Root's proposed remedy is viable: retain exact existing fixture source and `/data.json` route, and extend only `refresh.capture` with an optional locator-policy port alongside its proposed clock. Its default remains the existing `metadataUrl`. The fixture composition supplies an exact precompiled-locator guard and the injected fixture fetch bridge; neither is exposed as a user-selectable CLI port. A custom policy must require an explicitly supplied callable fetch implementation, rejecting omitted/undefined input before global fetch could be selected. This prevents accidental default transport use; the fixed fixture composition still needs separate zero-live-port evidence. Update the two-path amendment's currently clock-only authority to name this seam and paired-port check. Preserve the original finding and add exact-locator rejection, missing-fetch, default-compatibility and actual-result-consumption tests.

The other corrected contracts are coherent for bounded local engineering. Actual collector execution/result consumption, separate source observation and capture recording clocks, whole scheduler transaction records, shared poison propagation, parent/child reconciliation, lossless Map replay and one-attempt settlement address the earlier conditions. A durable response without a wrapper outcome is processed through the real collector on restart; an unknown delivery outcome remains unresolved without automatic redelivery. Memory state is explicitly a reducer, while the disk journal and referenced objects govern recovery.

Implementation review should preserve several inherited details. The origin governor has its own default wall clock and private per-origin state; inject the fixture clock and rely on journaled delivery reservations for cross-restart bounds without claiming durable managed pacing. The bridge must honor cancellation or bounded timeout settlement, since the legacy timer alone cannot stop an injected signal-ignoring port. Connector run/page state is keyed by execution ID, but membership/checkpoint maps are keyed by source ID; keep that sharing explicit and preserve child-specific results. Use a null body for the 304 Response and retain the real legacy `http_failed` limitation.

All **52 baseline source pins match**. An independent static relative-import walk recovered the same **28 modules**. The research module's import does not invoke its CLI/provider path under the existing exact `argv[1]` guard. Its unused child-process capability still belongs in runtime boundary review; pin the final changed closure, schemas, fixtures, policy, runtime and lockfile at the actual producer identity.

This was static source and retained-evidence review. No implementation, tests, dispatch, repository changes or requirement acceptance occurred. The exact finding, remedy conditions and evidence pins are retained in `review.json`.
