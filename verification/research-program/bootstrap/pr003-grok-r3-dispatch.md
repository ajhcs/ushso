# PR003 R3: two remaining packet-contract defects

Use Grok Co-Engineer for one bounded additive correction. The project and selected provider are already authorized. The previous R2 run completed normally and its two commits are published in draft PR15; this is a new assignment on that reviewed result. Root owns review, publication, integration and deployment.

Source repo: `/mnt/d/worktrees/plumbob/.ushso-research-program-20260910-worktrees/ushso-pr003-deepseek-20260910t220020z`.
Required starting HEAD: `5115170093512b06b21c01725bb982e457797efe`.
Required starting tree: `24fe636b0370e2e7b9c166d6f1c6bed520ed033e`.
Accepted direct PR003 dependencies: PR001 `035465f3f16d15f02467679d96451d4440a36ca8`; PR002 `5647e81457b606bb36456e75c48f990ce21e9c6c`.
The wrapper creates the managed worktree and owns its lifecycle and writer lease. Verify its starting head; do not reconstruct tokens or duplicate the harness.

Read repository instructions, EXECUTION.md, the PR003 packet and current schema README. The new independent final review is `/mnt/d/tmp/plumbob/ushso-research-program-20260910/pr003-grok-r2-independent-review/receipt.json`, SHA256 `91924040dd21957d55eab626d19abbfb86b9243b56b6c4caa12635314aae4681`.
Root independently ran all registered checks: 32 handoff tests, nine inventory tests, 12 schema cases, default handoff and PR guidance passed. The prior full fractional timestamp, large immutable Git snapshot, dangling symlink, exact-file/directory scope and provenance corrections passed their decisive checks. Preserve those behaviors.

Fix only these two remaining contract defects:

1. **PR003-R2-F6: malformed collection items.** Appending `null` to any of `commands`, `artifacts`, `source_identities`, `acceptance_results`, `failures_and_skipped_checks` or `unresolved_claims` in a valid packet still throws TypeError. The structural guard validates collection shape but later traversals dereference null items. A syntactically valid but schema-invalid packet must return a structured `outcome: rejected` report with `schema_invalid`; CLI exit must be 1, not an operational-error exit 2. Guard semantically traversed item shapes before traversal, or make those traversals safe. Preserve named semantic diagnostics for structurally safe incomplete packets where existing tests rely on them. Add meaningful API and actual CLI cases covering the six failing collections, malformed non-object elements and the passing valid-packet control. Do not swallow actual file/Git operational errors as packet success.
2. **PR003-R2-F7: documented wildcard ownership.** `tests/*.mjs` currently rejects `tests/one.mjs`, while `tests/**/*.mjs` and `*.mjs` can throw invalid-RegExp errors. Translate wildcard tokens separately from escaping literal regex characters. Preserve documented slash-aware `*` (one segment) and `**` (zero or more segments), including an interior globstar with no intervening directory. Keep exact-file, `dir/` and suffix `dir/**` behavior and boundary rejection. Add direct matcher and completed-packet scope cases; do not merely remove glob documentation or accept all paths. No additional wildcard features are required.

Root reproductions are `/mnt/d/tmp/plumbob/ushso-research-program-20260910/pr003-controller-review/5115170-semantic-host/receipt.json` (SHA256 `c6503896a31cc45732c19003b87f84965ed3af661702a9f9ea18b6ce7a671a2e`) and `.../pr003-controller-review/5115170-owned-path.json` (SHA256 `d5be04adbdb2c2cea16b05bea232bd21115a60d9cf2eccdd99e490804ff279b8`). Diagnostic scripts report defects and may have an outer zero exit despite `changes_requested`; meaningful new regressions must assert corrected outcomes.

Owned paths only: `scripts/research-program/check-handoff.mjs`, `tests/research-program/handoff.test.mjs`, `docs/research-program/handoffs/schema/`, `verification/research-program/pr-003/`, and `docs/research-program/handoffs/PR-003.json`. No root package files, test-inventory files, other PRs, plan/ledger, frozen inputs, CI/WP0, runtime configuration or production. No broad refactor is requested.

Keep all 38 existing command-receipt and evidence-index files at the starting head byte-identical. Add an R3 index and actual new command receipts; preserve the R2 index as historical, including its old artifact hashes. Current handoff, current task binding and changed code/fixtures may update within scope. Bind the new managed branch/owner honestly, exact starting head/tree and the controller review/dispatch input. Maintain the stated boundary: local task-binding consistency does not authenticate the producer or replace controller dispatch review. Keep the pending final head typed before commit; root externally binds the final commit/tree afterward.

Make one functional correction commit and, if needed, one additive evidence commit (C-003-2-R3 / C-003-3-R3). Run the focused handoff suite, schema helper, registered `npm run test:research-program`, and the default checker on the current handoff. Use Node >=22.15 and pinned npm11.19.1 at `/mnt/d/tmp/plumbob/ushso-research-program-20260910/tooling/node_modules/.bin/npm`. Do not copy or replace node_modules manually. Honor TMPDIR and clean task-owned fixtures.

Full combined npm test/build/CF/hosted CI remains root's integration work after PR085's active CI v1.4 correction. Record that limitation rather than editing its files or claiming those checks ran. Return exact final HEAD/tree, actual test counts and command results, new evidence paths, historical byte-preservation counts, remaining limitations and clean/released-writer status. Do not push, merge, deploy or mark PR003/R01–R16 accepted.
