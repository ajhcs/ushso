{
  "type": "reviewer_harness_assumption_correction",
  "product_files_changed": false,
  "initial_report_preserved": "object-integrity-review.json.initial",
  "items": [
    {
      "check": "initial_validation_failure_retained_and_recorded",
      "cause": "Validator stdout is a validation report with ok/outcome/findings, not the enclosing shell command receipt. Check its rejected outcome and timestamp-only findings instead of an absent exit_code field."
    },
    {
      "check": "accepted_and_assignment_critical_task_fields_unchanged",
      "cause": "Combined parent already contains a separately scoped PR086 continuation, while integration parent keeps original accepted PR086 at its top level. Verify continuation bindings unchanged from combined parent and original accepted identity preserved in its named prior acceptance snapshot."
    },
    {
      "check": "unrelated_tasks_match_one_parent_exactly",
      "cause": "PR086 combines current_input_correction metadata from both parents by documented merge. Check each value against parents, keep combined continuation task fields otherwise exact, and verify prior accepted identity."
    }
  ]
}
