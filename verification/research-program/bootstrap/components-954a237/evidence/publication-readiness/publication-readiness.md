The publication payload for `954a237a8984d06f5ea0ab15f83c71ce210bf09a` is locally reviewable. No public mutation was performed. The exact local gate passed all ten stages on the first attempt with one build; final metadata review, 23 custom probes and 12 Chrome regressions passed. Public publication and hosted CI remain pending explicit scoped authorization.

| Target | Current published head | Candidate |
|---|---|---|
| Draft #23 / PR-006 | `5d8a8dd6d2e9d60d5b18fa1f69a09533c7e0d9e9` | `a153a3d5e734fee17d9016b8d36b0f94006f24fe` |
| Draft #24 / PR-008 | `a50f5fae017f894d8bc46be001e6c5d12853db01` | `09e312c9fb8767656ba6f6dcb0f996510098842d` |
| Draft #12 / integration | `9e00d4e09514e1dd30672e19a3cbef4171955b00` | Keep current until combined acceptance |
| New combined draft | Branch/PR absent | `954a237a8984d06f5ea0ab15f83c71ce210bf09a` / tree `a781e9801224af92b51389fb898f0b67ab261982` |

GitHub snapshots at 04:00–04:04 UTC show all three existing PRs open and draft, with failed `npm test` in `node22-compatibility` and skipped `build-and-dry-run`. None of those old runs qualifies the corrected heads. The repository is public, and neither main nor integration currently has branch protections or rulesets. CI has no artifact-upload step. Do not infer program acceptance from GitHub's mergeable flag or absence of required server-side checks.

The final content audit inspected 48 commits and 556 new reachable blobs, including every gzip and eight screenshots: 10,497,458 stored bytes, 35,450,873 decoded bytes, no archive gaps. Four gitleaks findings were non-secret release-gate candidate hashes. Credential-field review found only dependency names/prose authorization. No credential/PHI/production-environment payload, new corpus rows, generated build/dependency directories or unexpected binary formats were found. 195 objects retain host paths/internal task identifiers as provenance. Largest blob is the historical gate receipt: 2,327,907 compressed bytes. Keep prior evidence bytes intact; the full blob inventory and redacted findings are retained locally.

Recommended route, after the controller verifies the actual action authorization:

1. Re-read the remote heads and compare them with `payload-destination-manifest.json`. Publish the combined head and optionally the two exact producer heads using ordinary atomic pushes. The prepared three-ref push updates only the two task branches and creates the combined branch; it does not advance integration or main. Push producer heads before the bundle merge if #23/#24 should name the finalized metadata heads.
2. Create one draft combined PR from `codex/ushso-pr006-pr008-combined-20260911` to the existing integration branch at `9e00d4e09514e1dd30672e19a3cbef4171955b00`. This base is already an ancestor of the candidate, so the merge tree should equal `a781e9801224af92b51389fb898f0b67ab261982`. The manifest contains explicit argv and body files; none was executed. Keep #12 draft. Update #23/#24 bodies only if their published heads match the prepared final heads; isolated branch CI is not joint-composition evidence.
3. Require both combined CI jobs to succeed. Capture their actual checkout commit from checkout logs and query its Git tree. The run's head SHA alone is insufficient. Require both checked-out trees to equal `a781e9801224af92b51389fb898f0b67ab261982` and retain job/run URLs, attempt numbers and the first-pass results. Refresh the body with the completed exact local gate and review links.
4. Only after those checks and controller acceptance, mark the combined PR ready and merge with `gh pr merge COMBINED_NUMBER --repo ajhcs/ushso --merge --match-head-commit 954a237a8984d06f5ea0ab15f83c71ce210bf09a`. Do not use auto/admin, squash, rebase or delete-branch flags. Recheck base/head immediately before merge; a changed resulting tree needs renewed qualification.
5. Read back the real merge and component PR states. For each of the combined number, 23 and 24, use `gh pr view NUMBER --repo ajhcs/ushso --json number,state,mergedAt,mergeCommit,headRefOid,baseRefName,url` and `gh api repos/ajhcs/ushso/pulls/NUMBER --jq '{number,merged,merged_at,merge_commit_sha,head_sha:.head.sha,base_sha:.base.sha}'`. Retain these responses. Before a PR is merged, `merge_commit_sha` is a synthetic test commit; only the post-merge state establishes the actual merge identity. [GitHub REST semantics](https://docs.github.com/en/rest/pulls/pulls#get-a-pull-request).
6. Query the combined merge object with `gh api repos/ajhcs/ushso/git/commits/ACTUAL_MERGE_SHA`, verify its tree equals `a781e9801224af92b51389fb898f0b67ab261982`, and verify its parents include the reviewed head and observed integration base. Fetch the actual integration ref under the normal scoped Git authorization, then prove `git merge-base --is-ancestor a153a3d5e734fee17d9016b8d36b0f94006f24fe ACTUAL_INTEGRATION_SHA` and the corresponding `09e312c9fb8767656ba6f6dcb0f996510098842d` check. Store finalized producer heads, the accepted combined merge SHA, tree, review/gate/CI references and acceptance rationale separately.
7. GitHub can mark other PRs merged when their heads become reachable through another merge; that marking does not prove their own checks passed. Current docs describe this as possible, so observe #23/#24 rather than assuming their badges or merge SHAs. The target here is non-default integration, an additional reason to verify behavior. [GitHub indirect merges](https://docs.github.com/en/pull-requests/reference/pull-request-merges#indirect-merges). If GitHub leaves them open, preserve the actual status and record their accepted bundle linkage. Do not merge #12/main merely to change a badge, invent an individual GitHub merge SHA, or silently equate an old published head with a finalized producer head.
8. Once component acceptance is explicit in the live ledger, bind PR-009 to the actual accepted integration/merge identities. If acceptance used a common bundle merge, record that common identity and explicit per-component lineage; do not fabricate two distinct merges. Keep any separately observed GitHub merge fields distinct. Verify PR-009's dependency map against the live ledger, retain `ushso-canonical-json.v1`, and exclude `911aff5` as a producer head rather than an accepted merge. No R01–R16 or production acceptance follows from this component closure.

The draft bodies and manifest now record the passed local gate and keep hosted CI pending. Final peer/browser/probe reports and the completed gate receipt are local scratch artifacts outside this scanned Git candidate. Their eventual publication needs a bounded artifact manifest; avoid unreviewed broad scratch uploads. The controller can append actual results and links to the prepared body files without another publication-readiness investigation, provided the Git head/tree remains unchanged and newly attached evidence is checked.

Prepared files: `combined-draft-body.md`, `pr-23-body.md`, `pr-24-body.md`, `pr-12-checkpoint-body.md`, `payload-destination-manifest.json`, `findings-disposition.json`, and `audit-954a237/payload-manifest.json`. Draft body files contain real newlines and no command interpolation.
