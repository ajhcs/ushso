# PR-003 R5: isolate handoff fixtures from the moving execution ledger

One bounded Grok Co-Engineer correction within already authorized PR-003. Source repo is `/mnt/d/worktrees/plumbob/.ushso-research-program-20260910-worktrees/ushso-pr003-deepseek-20260910t220020z`. Required start head is `d1c42eab33e1c21edf805036464f2b64667a489a`, tree `d765aa66a6bc360a3c03a86eaf54b6366ccb2be1`. Accepted direct dependency merges: PR-001 `035465f3f16d15f02467679d96451d4440a36ca8`; PR-002 `5647e81457b606bb36456e75c48f990ce21e9c6c`. Managed wrapper supplies the isolated worktree, lifecycle and writer lease. Verify the exact starting head; do not reconstruct the harness or its tokens.

Read repository instructions, EXECUTION.md, PR-003 and schema README. Root reviewed R4: 42 handoff tests, nine inventory tests, schema/guidance/current handoff and exact artifacts passed at d1c42ea. Root's 15 independent composite-glob cases passed the same functional checker. Preserve all of that behavior and the actual prior provider history.

The actual combined candidate exposed a fixture isolation defect. Root composed unchanged PR-003 d1c42ea, PR-004 323dfe5 and PR-085 4f90157 onto root 0c29fdb. Combined head `062a980e3c206856db9411f6c31c15127f614c9e`, tree `f3089e022a8aa6811f6267bda9d2acbd039e0741`, lives in `/mnt/d/worktrees/plumbob/.ushso-research-program-20260910-worktrees/ushso-pr003-pr004-pr085-composition-20260911` (READ ONLY). The real registered suite reported 57 tests and exit 1. Positive PR-003 fixture tests fail with binding_conflict: fixture task-binding base f62ce35481cc572e9aad054049c700aac6378f58 disagrees with the live execution-ledger base 0b28d036f1df4b5248732bc62f68edd79f14d06f. Production PR-003 and PR-004 current handoffs themselves pass on that combined head; this defect is in fixtures coupled to mutable controller state. Root receipt `/mnt/d/tmp/plumbob/ushso-research-program-20260910/component-composition-initial/receipt.json` SHA256 `2c6949e914ed59686739e32c0b3666b209c91d15e63806c70fc8ba7f7a8592e1`; failing suite stdout in the same directory `combined-research-tests.stdout` SHA256 `4cc879396d57606144ebf6a540ae0ec25466b9c222f17eac81bbd77bfc894b69`.

Fix only the fixture-context coupling. Make positive, expected-negative, unavailable, malformed-packet, immutable Git-snapshot and actual CLI checks run against an explicit controlled fixture ledger/context that cannot inherit the controller's changing PR task base. Preserve actual production ledger/base/dependency conflict detection and tests that demonstrate it. Do not weaken the validator, suppress conflicts, remove tests, add generic approval fallbacks, mutate the real ledger or mechanically repin historical command evidence to the current ledger. Avoid hardcoding just the latest moving root base; the suite must remain valid after a later legitimate ledger update. A small ignored fixture root inside the assigned repository can carry controlled files while ordinary read-only Git lookup still resolves the containing repository; no copied node_modules, Git-metadata workaround or new development checkout is necessary.

Regression must include the observed mismatch and another different plausible ledger base. Demonstrate the controlled fixture suite remains valid and a deliberately conflicting ledger within that controlled context still rejects with binding_conflict. Preserve module-authoritative schemas, path/symlink containment, pending-head semantics and actual CLI exit 0/1/2 behavior. Run focused and registered tests on your own isolated branch, plus a bounded reproduction using the captured root 0c29fdb ledger only as test data. Do not edit or run writer commands in root or the combined worktree.

Owned paths: `tests/research-program/handoff.test.mjs`, `verification/research-program/pr-003/`, `docs/research-program/handoffs/PR-003.json`, `docs/research-program/handoffs/schema/`. `scripts/research-program/check-handoff.mjs` may change only if a small explicit context-support change is essential and leaves default real-ledger validation strict; explain it and test the strict default. No root package/lock, CI/WP0/WP11, other PR code, actual plan/ledger, frozen cohorts, acceptance thresholds, configuration or production changes.

Preserve all 53 existing command-receipt/evidence-index files byte-for-byte. Add R5 evidence and actual command receipts; refresh only current fixture/hash references and the current handoff/binding as needed. Identify Grok and its actual interface model honestly, with the exact new managed branch, base and dependency hashes. Prior R4/DeepSeek/Grok records remain historical. Use one functional commit and one additive evidence commit if needed. Node >=22.15 and npm 11.19.1 is available at `/mnt/d/tmp/plumbob/ushso-research-program-20260910/tooling/node_modules/.bin/npm`; honor TMPDIR and clean task-owned temporary fixtures.

Run focused and registered handoff tests, schema helper, PR guidance and the default current handoff check. Root owns the final combined gate and publication. WP11 scope review and PR-085 package.json historical-context correction are separate and outside your assignment. Do not claim full npm test/build/CF or hosted CI ran. Return exact final head/tree, actual checks, evidence hashes, historical preservation count, limitations and clean/released status. Do not push, merge, deploy or mark acceptance.
