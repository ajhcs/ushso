# Remaining PR-002 implementation contract

This controller note supplies public inputs and review constraints for the C-002-2/C-002-3 owner after acceptance of the additive C1 correction. It does not authorize a dependency bypass or constitute an implementation receipt.

## Fixed inputs

- Preserve Grok C-002-1 commit 44506be8052e1054641048de68893fc41526b664 and its additive correction. Do not change the 100-product selection, ten domains, or the 3,434 baseline record identities.
- Sealed public evaluator manifest: evaluator-freeze-manifest-sealed-v1.json, SHA256 142458d686e1cb5e9677b6a5eaa619955c78ef9a4d51735207d4ad19bf26c9ef, in this task's cohort-inputs directory. Copy the public manifest byte-for-byte into the PR evidence and reference its hash from tasks.json. Raw questions, labels, answer IDs and private files are outside implementation scope. The public opaque task index has 40 tasks, four per domain, with 10 novice, 17 advanced and 13 machine entries. This is an input freeze, not scientific relevance gold or empirical acceptance.
- All R01-R16 remain unaccepted. Preserve the MASTER-PLAN.md thresholds exactly and give each requirement a denominator, numerator/pass predicate, source/generation identity, evidence owner, evidence destination, required later materialization and status. Explicitly block qualification until every conditional frame has been materialized and independently frozen before tuning or measurement.
- R07: current-generation present-source recall@10 task macro >=0.90; independently judged precision@5 task macro >=0.80; zero critical forbidden matches. All returned top-five items require blind independent judgments. Report target/item micro alongside. Full-intention recall covers all 49 target slots across all 40 tasks and is reported separately without a numeric threshold. The 3,434 catalog IDs are not a retrieval relevance denominator.
- R14 public guidance: cohort-inputs/c2-r14-denominator-freeze-protocol-v2.md SHA256 3a35ee6662f939bf53e9f8c5005079ac09982372d80c9b385a9beb9c52e525c7. Apply the stage sequence precisely: C2 publishes the protocol/row; PR020 supplies the residual frame; PR027 C1 freezes independent targets before comparison calls; PR027 C2/C3 execute and evaluate model outputs; R14 quality gates promotion. Actual model results are never a prerequisite for creating the PR027 comparison itself. The illustrative149iid count is not an adopted sample-size/CI threshold.
- R14: the 40 retrieval tasks do not constitute a held-out scientific claim gold set. A later independent claim frame must be frozen from actual deterministic residual evidence before model comparison or routing tuning. Keep held-out precision >=0.98, zero critical scientific errors, 100% accepted-claim citation/schema checks, actual sample sizes, confidence intervals and per-source/task-class results. Model agreement cannot create gold labels. Paid enrichment requires a selected concrete budget and credentials; offline work may proceed.
- R12: eight novice/eight advanced actual participants, distinct from implementers, and >=85% completion in each group with zero critical misinterpretation. Freeze participant-task assignments and success rules before the study; missing sessions and untested tasks remain visible. Simulations do not count.
- R15: two actual scheduled cycles and 14 actual elapsed observation days. No generated dates or synthetic operation can satisfy it.

## C-002-3 directory custody and freeze order

Use retained authoritative public-directory metadata only; no endpoint requests or new collection are needed for selection. Inputs in cohort-inputs:

- input-index.json SHA256 defd18e1fd72671b6c842845eeb2b6ef102526befd9c26cf251fa117dfc39587.
- hospital-directory-identities.json SHA256 3cf22001c0d1d831174f6aabe678c2adf508d8cc41911a13c220ed276c619fdd: 5,419 unique facility IDs with name/city/state/type/ownership and source row pointers.
- hospital-directory-metadata.json SHA256 31eea397b8bfe8207168cefda23256eb9cb3d5e49158d221eda8465a2d8e5f8c. Read and independently rehash the file; the controller dispatch supplies the authoritative exact digest.
- payer-directory-identities.json SHA256 7ecd1fe538ba0d49d04d6b418541ff1824f4aba5dc3f6aa78242e63e028df26c: 156 exact HIOS issuer IDs, legal names/states and source XLSX cells.
- payer-issuer-directory.xlsx SHA256 09a5d4a33b5825feb3f4943e3cb6ebd9577b8665dd6def15ac01e12b273b5f0b. It is the CMS PY2026 QIS issuer list, not a TiC MRF index.
- receipts-20260910T155046Z.json SHA256 09d4ac6b29f6d8c041053bbb956d46a34c2cfef6c78157c53b2712b1aa88e9c1.

Consume the independently sealed selection from pr002-pilot-controller-review/pilot-selection-input-seal-v1.json (SHA256 f74892bbb9bb38619e985cb20e2dd9c6478eba0d7b0bcb02af234f70a1a03060). Its exact 25 hospital IDs and 10 HIOS issuer candidate IDs are fixed; do not select again. The source specification, unchanged producer artifacts and selected row/cell fixtures live in pr002-pilot-selection-preparation and are individually hash-bound by the seal. Root independently reproduced the ordering and resolved all 156 payer workbook rows; 523 checks passed. Preserve the candidate artifacts and seal byte-for-byte while adding portable repository-relative references. Historical candidate status pending_root_adjudication is superseded only for input selection by the external root seal. Publish the selected row/cell evidence as small fixtures, including exact original wire values, authoritative directory locator, capture receipt/hash and selector. Do not commit the whole hospital directory or perform endpoint tests. Retain unavailable/ineligible/unresolved selections with their original IDs and reasons; no endpoint-driven replacement.

A HIOS issuer is a selected candidate for later reporting-entity binding, not proof of a TiC reporting entity or file. Preserve this unresolved distinction explicitly. PR-046/PR-049 consume the frozen IDs and resolve actual MRF locators without reselecting pilots. Their acceptance must reconcile the original 25/10 denominator even if bindings fail or entities share a file. MRF schema repositories and enforcement datasets do not establish any selected entity's rate-file locator.

Include the twelve named expansion families with actual named intake PR/atomic task, source identity expectation, candidate locator and its evidence limits. State-facility licensure requires a state authority later; federal certification context is not a state license. A secondary APCD directory is navigation evidence only. An existing look-alike SVI record does not establish the exact CDC/ATSDR family.

## Handoff and verification

C-002-2 and C-002-3 remain two distinct planned commits; additive review corrections are separately recorded. Keep mixed authorship honest: Grok C1, Luna Max correction, and Grok C2/C3 implementation if dispatched through that interface. Extend the replayable builder/verifier for the final schema without invalidating the C1 historical receipts. Verification must check preservation of all original IDs, positive and negative selector cases, public/private manifest boundaries, frozen pilot counts/uniqueness/exact directory evidence, all sixteen acceptance rows and no deletion-based success. Preserve command failures, actual timing and exit codes. Root independently reviews and replays the final head before publication/integration.
