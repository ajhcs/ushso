# Isolated fixture validation context and runtime packets. These stay inside
# the assigned repository so ordinary Git lookup still resolves the containing
# worktree, but they must not inherit the live controller execution ledger.
.context/
.runtime-handoff-*/
