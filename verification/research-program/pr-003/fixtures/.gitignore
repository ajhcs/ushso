# Isolated fixture validation context, runtime packets, and portable scratch.
# These stay inside the assigned repository so ordinary Git lookup still
# resolves the containing worktree, but they must not inherit the live
# controller execution ledger. Scratch children are task-owned and cleaned
# individually; an existing unrelated directory is never recursively removed.
.context/
.scratch/
.runtime-handoff-*/
