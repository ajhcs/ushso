{
  "name": "@ushso/verification-wp3",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fail-closed aggregate verification for the WP3 PostgreSQL and Cloudflare/Neon infrastructure foundation.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "validate": "node tools/validate-wp3.mjs",
    "test:db:local-docker": "npm test --prefix db"
  }
}
